apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: builder

bases:
- ../../bases/registry

patches:
- registry.yaml

resources:
- certificate.yaml
- ingress.yaml
- issuer.yaml
- namespace.yaml
